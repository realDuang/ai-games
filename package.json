{
  "name": "ai-games",
  "private": true,
  "version": "1.0.0",
  "description": "AI Games Monorepo",
  "scripts": {
    "dev:mine": "pnpm --filter mine-sweeper dev",
    "build:mine": "pnpm --filter mine-sweeper build",
    "dev:snake": "pnpm --filter snake dev",
    "build:snake": "pnpm --filter snake build",
    "lint": "pnpm --filter mine-sweeper lint",
    "lint:snake": "pnpm --filter snake lint",
    "prepare": "husky",
    "lint-staged": "lint-staged"
  },
  "devDependencies": {
    "typescript": "^5.5.3",
    "husky": "^8.0.0",
    "lint-staged": "^15.0.0"
  },
  "lint-staged": {
    "games/*/src/**/*.{js,jsx,ts,tsx}": [
      "pnpm lint"
    ]
  }
}